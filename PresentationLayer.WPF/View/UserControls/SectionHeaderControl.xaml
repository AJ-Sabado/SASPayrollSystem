<UserControl x:Class="PresentationLayer.WPF.View.UserControls.SectionHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:userControls="clr-namespace:PresentationLayer.WPF.View.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Section Title -->
        <TextBlock Grid.Column="0"
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontFamily="{StaticResource PoppinsBold}"
                   FontSize="24"
                   Margin="0,0,0,30" />

        <!-- Edit Button -->
        <Button Grid.Column="1"
                Tag="{Binding ButtonName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Edit" Height="20" Width="20" Foreground="White"/>
                <TextBlock Text="Edit" 
                           FontFamily="{StaticResource PoppinsSemiBold}" 
                           FontSize="14" 
                           Margin="5,0,0,0" 
                           Foreground="White"/>
            </StackPanel>
        </Button>
    </Grid>

</UserControl>
