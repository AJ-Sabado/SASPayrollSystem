<UserControl x:Class="PresentationLayer.WPF.View.UserControls.LabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontFamily="{StaticResource PoppinsSemiBold}" FontSize="14" Margin="2"/>
            <TextBox Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     FontFamily="{StaticResource PoppinsLight}" FontSize="12" Margin="2" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>
