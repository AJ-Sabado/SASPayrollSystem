<Window x:Class="PresentationLayer.WPF.View.Windows.PopUps.CustomMessageBox.PasswordPrompt_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:userControls="clr-namespace:SASPayrolSystemProject.View.UserControls"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Title="Enter Password"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Icon and Title -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0 0 0 10">
            <materialDesign:PackIcon Kind="Lock" Foreground="DarkSlateBlue" Width="40" Height="40"/>
            <TextBlock Text="Authorization Required"
                       FontFamily="{StaticResource PoppinsBold}" 
                       FontSize="24" VerticalAlignment="Center" Margin="10 0 0 0"/>
        </StackPanel>

        <!-- Custom Password Box -->
        <userControls:CustomPasswordBox Grid.Row="1"
                                        x:Name="customPasswordBox"
                                        Margin="0,10,0,10"
                                        HorizontalAlignment="Center"
                                        Width="360" Height="50" />

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Cancel"
                    Width="100"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click" Margin="0,0,15,0"/>
            <Button x:Name="btnConfirm"
                    Content="Confirm"
                    Width="100"
                    FontFamily="{StaticResource PoppinsBold}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
